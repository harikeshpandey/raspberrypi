{
  "name": "raspberry-pi-wifi-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Stream `iw dev wlan0 station dump` output to a browser over WebSocket.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
